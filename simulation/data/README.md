# Simulation data

The raw working database is not included in this publication candidate. Set the `WARDROP_PRIMARY_DB` environment variable to an authorized local copy, or place `simulation.db` in this directory.

The aggregate welfare tables used by the paper are available in the repository-level `results/` directory.
